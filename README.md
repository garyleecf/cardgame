# Card Game

Instructions:
    git clone https://github.mit.edu/sia/cardgame.git
    cd cardgame
    pip install -e .

In Python:
    import gym
    env = gym.make('gym_cardgame:threecard-game-v0')
    env.reset()
